# 3D Models

The current production-block printer is a procedural Three.js model in `src/App.jsx`.
It is modeled as a commercial flatbed / UV printer: cabinet, print bed, gantry rail,
moving carriage, control panel, vents, warning labels, caster wheels, and printed sheet.

No external 3D model file is currently required.
